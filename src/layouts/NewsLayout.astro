---
interface NewsLayout {
    title: string;
}

const { title } = Astro.props;
---

{title && <h2 class="flex flex-col text-center text-4xl font-bold p-4">{title}</h2>}
<section class="grid grid-cols-1 max-w-[1280px] mx-auto md:grid-cols-2 lg:grid-cols-3 gap-5 p-5">
    <slot />
</section>